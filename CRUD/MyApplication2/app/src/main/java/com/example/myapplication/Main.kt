package com.example.myapplication

fun main () {

    /*
    //Crear
    val creado = Barbero.crear("john@example.com",
        "password123",
        "John",
        "Smith ",
        "Johny",
        "Lore Ipsum",
        "948451298",
        "C:/Users/USER/Downloads/2.png",
        "2")
    if (creado) {
        println("Barbero creado con éxito.")
    } else {
        println("Error al crear el barbero.")
    }

    // Obtener todos los barberos
    val listaBarberos = Barbero.obtenerTodos()
    println("Lista de barberos:")
    for (barbero in listaBarberos) {
        println("${barbero.id}, ${barbero.correo}, ${barbero.password}")
    }

    // Actualizar un barbero
    val actualizado = Barbero.actualizar(21, "jane@example.com", "password321")
    if (actualizado) {
        println("Barbero actualizado con éxito.")
    } else {
        println("Error al actualizar el barbero.")
    }

    // Eliminar un barbero
    val eliminado = Barbero.eliminar(21)
    if (eliminado) {
            println("Barbero eliminado con éxito.")
    } else {
        println("Error al eliminar el barbero.")
    }


    //Servicio

    val creado = Servicio.crear("Corte de cabello",
        "Corte moderno",
        20.0,
        "T",
        3)
    if (creado) {
        println("Servicio creado con éxito.")
    } else {
        println("Error al crear el servicio.")
    }

    // Obtener todos los servicios
    val listaServicios = Servicio.obtenerTodos()
    println("Lista de servicios:")
    for (servicio in listaServicios) {
        println("${servicio.idServicio}: ${servicio.nombreServicio}, ${servicio.descripcionServicio}, ${servicio.precioServicio}, ${servicio.estadoServicio}, ${servicio.idBarbero}")
    }

    val actualizado = Servicio.actualizar(6, "Corte de cabello", "Corte clásico", 25.0, "F", 4)
    if (actualizado) {
        println("Servicio actualizado con éxito.")
    } else {
        println("Error al actualizar el servicio.")
    }

    val eliminado = Servicio.eliminar(6)
    if (eliminado) {
        println("Servicio eliminado con éxito.")
    } else {
        println("Error al eliminar el servicio.")
    }



    //Cliente

    val creado = Cliente.crear("John", "Doe", "john@example.com", "password123")
    if (creado) {
        println("Cliente creado con éxito.")
    } else {
        println("Error al crear el cliente.")
    }


    // Obtener todos los clientes
    val listaClientes = Cliente.obtenerTodos()
    println("Lista de clientes:")
    for (cliente in listaClientes) {
        println("${cliente.idCliente}: ${cliente.nombreCliente}, ${cliente.apellidoCliente}, ${cliente.correoCliente}, ${cliente.passwordCliente}")
    }

    // Actualizar un cliente
    val actualizado = Cliente.actualizar(21, "Jane", "Doe", "jane@example.com", "newpassword123")
    if (actualizado) {
        println("Cliente actualizado con éxito.")
    } else {
        println("Error al actualizar el cliente.")
    }

    // Eliminar un cliente
    val eliminado = Cliente.eliminar(21)
    if (eliminado) {
        println("Cliente eliminado con éxito.")
    } else {
        println("Error al eliminar el cliente.")
    }
    */

}